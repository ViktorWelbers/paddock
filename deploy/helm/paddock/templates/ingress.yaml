{{- if .Values.ingress.enabled }}
# Only the server API is exposed; the gateway is reached by sandboxes over
# the cluster network and needs no ingress.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  {{- with .Values.ingress.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts: [{{ required "ingress.host is required with ingress.tls" .Values.ingress.host | quote }}]
      secretName: {{ .Release.Name }}-ingress-tls
  {{- end }}
  rules:
    # No host = match-all: used by the k3d dev loop, where the ingress is
    # reached as localhost through the mapped loadbalancer port.
    - {{- with .Values.ingress.host }}
      host: {{ . | quote }}
      {{- end }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-server
                port:
                  number: {{ .Values.server.port }}
{{- end }}
